<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Programming on Sam Smith</title><link>/categories/programming/</link><description>Sam Smith (Programming)</description><generator>Hugo -- gohugo.io</generator><language>en-gb</language><lastBuildDate>Sun, 01 Mar 2020 00:00:00 +0000</lastBuildDate><atom:link href="/categories/programming/index.xml" rel="self" type="application/rss+xml"/><item><title>Rgeo — A Go package for basic, fast, local reverse geocoding</title><link>/rgeo/</link><pubDate>Sun, 01 Mar 2020 00:00:00 +0000</pubDate><guid>/rgeo/</guid><description>&lt;p>&lt;a href="https://github.com/sams96/rgeo">Rgeo&lt;/a> is a fast, simple solution for local
reverse geocoding, Rather than relying on external software or online APIs, rgeo
packages all of the data it needs in your binary. This means it will only ever
work down to the level of cities , but if that&amp;rsquo;s all you need then this is the
library for you.&lt;/p>
&lt;p>Rgeo uses data from &lt;a href="https://naturalearthdata.com">naturalearthdata.com&lt;/a>, if
your coordinates are going to be near specific borders I would advise checking
the data beforehand (links to which are in the files). If you want to use your
own dataset, check out
&lt;a href="https://github.com/sams96/rgeo/tree/master/datagen">datagen&lt;/a>.&lt;/p>
&lt;h2 id="key-features">Key Features&lt;/h2>
&lt;ul>
&lt;li>&lt;strong>Fast&lt;/strong> - So I haven&amp;rsquo;t &lt;em>actually&lt;/em> benchmarked other reverse geocoding tools
but on my laptop rgeo can run at under 800ns/op.&lt;/li>
&lt;li>&lt;strong>Local&lt;/strong> - Rgeo doesn&amp;rsquo;t require pinging some API, most of which either cost
money to use or have severe rate limits.&lt;/li>
&lt;li>&lt;strong>Lightweight&lt;/strong> - The rgeo repo is 141MB, which is large for a Go package but
compared to the 800GB needed for a full planet install of
&lt;a href="https://nominatim.org/release-docs/latest/admin/Installation/#hardware">Nominatim&lt;/a>
it&amp;rsquo;s miniscule.&lt;/li>
&lt;/ul>
&lt;h2 id="installation">Installation&lt;/h2>
&lt;p>Download with&lt;/p>
&lt;pre>&lt;code>go get github.com/sams96/rgeo
&lt;/code>&lt;/pre>
&lt;p>and add&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">&lt;span style="font-weight:bold">import&lt;/span> &lt;span style="color:#b84">&amp;#34;github.com/sams96/rgeo&amp;#34;&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>to the top of your Go file to include it in your project.&lt;/p>
&lt;h2 id="usage">Usage&lt;/h2>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">r, err &lt;span style="font-weight:bold">:=&lt;/span> &lt;span style="color:#900;font-weight:bold">New&lt;/span>(Provinces10, Cities10)
&lt;span style="font-weight:bold">if&lt;/span> err &lt;span style="font-weight:bold">!=&lt;/span> &lt;span style="font-weight:bold">nil&lt;/span> {
&lt;span style="color:#998;font-style:italic">// Handle error
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span>}
loc, err &lt;span style="font-weight:bold">:=&lt;/span> r.&lt;span style="color:#900;font-weight:bold">ReverseGeocode&lt;/span>([]&lt;span style="color:#458;font-weight:bold">float64&lt;/span>{&lt;span style="color:#099">141.35&lt;/span>, &lt;span style="color:#099">43.07&lt;/span>})
&lt;span style="font-weight:bold">if&lt;/span> err &lt;span style="font-weight:bold">!=&lt;/span> &lt;span style="font-weight:bold">nil&lt;/span> {
&lt;span style="color:#998;font-style:italic">// Handle error
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span>}
fmt.&lt;span style="color:#900;font-weight:bold">Println&lt;/span>(loc)
&lt;span style="color:#998;font-style:italic">// Output: &amp;lt;Location&amp;gt; Sapporo, Hokkaido, Japan (JPN), Asia
&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>First initialise rgeo using &lt;code>rgeo.New&lt;/code>,&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">&lt;span style="font-weight:bold">func&lt;/span> &lt;span style="color:#900;font-weight:bold">New&lt;/span>(datasets &lt;span style="font-weight:bold">...&lt;/span>&lt;span style="font-weight:bold">func&lt;/span>() []&lt;span style="color:#458;font-weight:bold">byte&lt;/span>) (&lt;span style="font-weight:bold">*&lt;/span>Rgeo, &lt;span style="color:#458;font-weight:bold">error&lt;/span>)
&lt;/code>&lt;/pre>&lt;/div>&lt;p>which takes any non-zero number of datasets as arguments. The included datasets
are:&lt;/p>
&lt;ul>
&lt;li>&lt;code>Countries110&lt;/code> - Just country information, smallest and lowest detail of the
included datasets.&lt;/li>
&lt;li>&lt;code>Countries10&lt;/code> - The same as above but with more detail.&lt;/li>
&lt;li>&lt;code>Provinces10&lt;/code> - Includes province information as well as country, so can
still be used alone.&lt;/li>
&lt;li>&lt;code>Cities10&lt;/code> - Just city information, if you want provinces and/or countries as
well use one of the above datasets with it.
Once initialised you can use &lt;code>ReverseGeocode&lt;/code> on the value returned by &lt;code>New&lt;/code>,
with your coordinates to get the location information. See the &lt;a href="https://pkg.go.dev/github.com/sams96/rgeo">Go
Docs&lt;/a> for more information on usage.&lt;/li>
&lt;/ul>
&lt;p>Then use &lt;code>ReverseGeocode&lt;/code> to get the location information of the given coordinate.&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">&lt;span style="font-weight:bold">func&lt;/span> (r &lt;span style="font-weight:bold">*&lt;/span>Rgeo) &lt;span style="color:#900;font-weight:bold">ReverseGeocode&lt;/span>(loc geom.Coord) (Location, &lt;span style="color:#458;font-weight:bold">error&lt;/span>)
&lt;/code>&lt;/pre>&lt;/div>&lt;p>The input is a &lt;a href="https://github.com/twpayne/go-geom">&lt;code>geom.Coord&lt;/code>&lt;/a>, which is just
a &lt;code>[]float64&lt;/code> with the longitude in the zeroth position and the latitude in the
first position (i.e. &lt;code>[]float64{lon, lat}&lt;/code>). &lt;code>ReverseGeocode&lt;/code> returns a
&lt;code>Location&lt;/code>, which looks like this:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">&lt;span style="font-weight:bold">type&lt;/span> Location &lt;span style="font-weight:bold">struct&lt;/span> {
&lt;span style="color:#998;font-style:italic">// Commonly used country name
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span> Country &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;country,omitempty&amp;#34;`&lt;/span>
&lt;span style="color:#998;font-style:italic">// Formal name of country
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span> CountryLong &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;country_long,omitempty&amp;#34;`&lt;/span>
&lt;span style="color:#998;font-style:italic">// ISO 3166-1 alpha-1 and alpha-2 codes
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span> CountryCode2 &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;country_code_2,omitempty&amp;#34;`&lt;/span>
CountryCode3 &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;country_code_3,omitempty&amp;#34;`&lt;/span>
Continent &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;continent,omitempty&amp;#34;`&lt;/span>
Region &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;region,omitempty&amp;#34;`&lt;/span>
SubRegion &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;subregion,omitempty&amp;#34;`&lt;/span>
Province &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;province,omitempty&amp;#34;`&lt;/span>
&lt;span style="color:#998;font-style:italic">// ISO 3166-2 code
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span> ProvinceCode &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;province_code,omitempty&amp;#34;`&lt;/span>
City &lt;span style="color:#458;font-weight:bold">string&lt;/span> &lt;span style="color:#b84">`json:&amp;#34;city,omitempty&amp;#34;`&lt;/span>
}
&lt;/code>&lt;/pre>&lt;/div>&lt;p>So, to put it all together:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="background-color:#fff;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-go" data-lang="go">r, err &lt;span style="font-weight:bold">:=&lt;/span> rgeo.&lt;span style="color:#900;font-weight:bold">New&lt;/span>(Countries110)
&lt;span style="font-weight:bold">if&lt;/span> err &lt;span style="font-weight:bold">!=&lt;/span> &lt;span style="font-weight:bold">nil&lt;/span> {
&lt;span style="color:#998;font-style:italic">// Handle error
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span>}
loc, err &lt;span style="font-weight:bold">:=&lt;/span> r.&lt;span style="color:#900;font-weight:bold">ReverseGeocode&lt;/span>([]&lt;span style="color:#458;font-weight:bold">float64&lt;/span>{&lt;span style="color:#099">0&lt;/span>, &lt;span style="color:#099">52&lt;/span>})
&lt;span style="font-weight:bold">if&lt;/span> err &lt;span style="font-weight:bold">!=&lt;/span> &lt;span style="font-weight:bold">nil&lt;/span> {
&lt;span style="color:#998;font-style:italic">// Handle error
&lt;/span>&lt;span style="color:#998;font-style:italic">&lt;/span>}
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.Country)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.CountryLong)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.CountryCode2)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.CountryCode3)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.Continent)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.Region)
fmt.&lt;span style="color:#900;font-weight:bold">Printf&lt;/span>(&lt;span style="color:#b84">&amp;#34;%s\n&amp;#34;&lt;/span>, loc.SubRegion)
&lt;span style="color:#998;font-style:italic">// Output: United Kingdom
&lt;/span>&lt;span style="color:#998;font-style:italic">// United Kingdom of Great Britain and Northern Ireland
&lt;/span>&lt;span style="color:#998;font-style:italic">// GB
&lt;/span>&lt;span style="color:#998;font-style:italic">// GBR
&lt;/span>&lt;span style="color:#998;font-style:italic">// Europe
&lt;/span>&lt;span style="color:#998;font-style:italic">// Europe
&lt;/span>&lt;span style="color:#998;font-style:italic">// Northern Europe
&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;h2 id="projects-using-rgeo">Projects using rgeo&lt;/h2>
&lt;p>Currently the only project that I know of is my own
&lt;a href="https://github.com/sams96/rgeoSrv">rgeoSrv&lt;/a>, which aims to wrap rgeo into a
microservice. I am also planning on writing a command line appication to apply
IPTC location tags to images that already contain geolocation information.&lt;/p>
&lt;h2 id="how-it-works">How it works&lt;/h2>
&lt;p>The data used by rgeo is made from a collection of GeoJSON files aquired from
&lt;a href="https://github.com/nvkelso/natural-earth-vector/tree/master/geojson">Natural Earth
Data&lt;/a>,
which are packaged into Go files by
&lt;a href="https://github.com/sams96/rgeo/tree/master/datagen">datagen&lt;/a>. The go files
contain functions which return byte slices that contain the base 64 encoded,
gzipped GeoJSON files. They&amp;rsquo;re packaged into function because that seems to be
the only way to have the Go compiler ignore them if they aren&amp;rsquo;t used (so your
program isn&amp;rsquo;t inflated by huge data files that you aren&amp;rsquo;t using).&lt;/p>
&lt;p>&lt;code>New&lt;/code> takes the datasets, decodes the data and parses the GeoJSON. It creates an
&lt;code>Rgeo&lt;/code> struct which contains an &lt;a href="https://github.com/golang/geo">s2&lt;/a> shape index,
a map to store the location information of each area and an s2 contains point
query. The shape index contains s2 polygons for each of the areas in the given
dataset, and the contains point query is what goes from the input coordinates to
a shape. The map is then used to get the location information from the shape.&lt;/p>
&lt;p>One thing I discovered through working on this is initialising s2 loops and
polygons is very slow and there&amp;rsquo;s no way to store them that doesn&amp;rsquo;t have to run
all the validation code upon loading them. I found that going from creating the
s2 types every time to using geom&amp;rsquo;s &lt;code>IsPointInRing&lt;/code> function yields a ~100x
speed increase. I used s2 types because I wrote rgeo to be used many times in a
program rather than a few, and s2&amp;rsquo;s contains point query is &lt;em>very&lt;/em> fast
(although I don&amp;rsquo;t have any benchmarks to hand).&lt;/p></description></item></channel></rss>